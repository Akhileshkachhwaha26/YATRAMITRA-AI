Reserved for local image/icon assets. Destination/hotel imagery currently comes from a keyword-matched
real-photo service (see src/utils/imageData.js) with an illustrated fallback (see src/components/visuals/Scene.jsx)
rather than bundled photos — drop files here and point REAL_IMAGES in src/utils/scenes.js at them to override.
